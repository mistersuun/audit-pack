{
  "name": "audit-pack",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test --config=tests/playwright/playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "xlsx": "^0.18.5"
  }
}
